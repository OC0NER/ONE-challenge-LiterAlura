spring.application.name=challenge-literatura
spring.datasource.url=jdbc:postgresql://${DB_HOST}/challenge-literatura
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
hibernate.dialect=org.hibernate.dialect.HSQLDialect

spring.jpa.hibernate.ddl-auto=update

#Habilita exhibicion de logs de SQL
#spring.jpa.show-sql=true

#Habilita formateo de los logs de SQL para facilitar lectura
#spring.jpa.properties.hibernate.format_sql=true